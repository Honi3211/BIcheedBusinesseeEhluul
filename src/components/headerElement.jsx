const HeaderEl = ({ text }) => {
  return (
    <span className="text-[16px] text-center font-bold cursor-pointer hover:underline transition duration-500">
      {text}
    </span>
  );
};
export default HeaderEl;
